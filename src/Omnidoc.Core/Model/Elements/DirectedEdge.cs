namespace Omnidoc.Model.Elements
{
    public class DirectedEdge : Edge
    {
        public DirectedEdge ( Element element, Element source, Element target ) : base ( element, source, target ) { }
    }
}